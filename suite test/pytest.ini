[pytest]
testpaths = hundidos_tests/tests
addopts = -ra
markers =
    test_id(id): stable traceability id for TFG VD2
    requirement(*refs): brief or acceptance criteria references
    metric(*refs): metric references such as M6, M7, M-operatividad
