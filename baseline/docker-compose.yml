version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/hundidos
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1  # Puedes ajustar según sea necesario
      - DJANGO_SETTINGS_MODULE=ecommerce.settings
    command: ["python", "manage.py", "runserver", "0.0.0.0:8000"]

volumes:
  sqlite_data:
